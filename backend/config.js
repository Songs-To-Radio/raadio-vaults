const  config = {
    NFT_STORAGE_TOKEN: "key goes here",
    DB_USERNAME: "radiovaults",
    DB_PASSWORD: "songstoradio",
    IPFS_GATEWAY: "https://nftstorage.link/ipfs",
    PORT: process.env.PORT || 2000,
    // DOMAIN: "https://radio-vault.herokuapp.com" || "http://localhost:2000",
    DOMAIN: "http://localhost:2000",
}

export default config;